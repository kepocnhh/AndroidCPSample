<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<!--    <permission android:name="test.android.cp.permission.provider"/>-->
    <uses-permission android:name="test.android.cp.permission.provider"/>

    <application
        android:label="CP/pf2">
        <provider
            android:name=".provider.FinalContentProvider"
            android:authorities="${applicationId}.authority.provider"
            android:exported="true"
            android:permission="test.android.cp.permission.provider"/>
    </application>
</manifest>
